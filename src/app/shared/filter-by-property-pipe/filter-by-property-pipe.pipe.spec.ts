import {FilterByPropertyPipePipe} from './filter-by-property-pipe.pipe';

describe('FilterByPropertyPipePipe', () => {
	it('create an instance', () => {
		const pipe = new FilterByPropertyPipePipe();
		expect(pipe).toBeTruthy();
	});
});
