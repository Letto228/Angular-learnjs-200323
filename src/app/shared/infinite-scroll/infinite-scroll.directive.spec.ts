import {EndlessScrollingDirective} from './infinite-scroll.directive';

describe('EndlessScrollingDirective', () => {
	it('should create an instance', () => {
		const directive = new EndlessScrollingDirective();
		expect(directive).toBeTruthy();
	});
});
